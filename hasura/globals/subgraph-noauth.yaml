kind: Subgraph
version: v2
definition:
  name: globals
  generator:
    rootPath: .
  includePaths:
    - auth-config-noauth.hml
    - compatibility-config.hml
    - graphql-config.hml
  envMapping:
    JWT_SECRET:
      fromEnv: JWT_SECRET
