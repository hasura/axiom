kind: Subgraph
version: v1
definition:
  generator:
    rootPath: .
  envFile: .env.supergraph_globals
  includePaths:
    - auth-config.cloud.hml
    - compatibility-config.cloud.hml
    - graphql-config.cloud.hml
  name: globals
